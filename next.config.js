/** @type {import('next').NextConfig} */

const nextConfig = {
  output: 'export', // Enables static HTML export
  // No assetPrefix or basePath needed for Firebase Hosting
};

module.exports = nextConfig;
